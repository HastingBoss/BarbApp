{
  "name": "barberia-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "install:all": "npm install --workspaces"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
